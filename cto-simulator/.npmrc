# Allow install when peer deps don't match (e.g. eslint@10 with eslint-plugin-react-hooks@7 which only declares ^9).
# Needed for Vercel/CI; ESLint 10 works with the plugin at runtime.
legacy-peer-deps=true
